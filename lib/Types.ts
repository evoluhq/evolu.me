/**
 * React Native for the Web props types are different than React Native props,
 * so we silence errors with this helper, which will be removed in the future.
 *
 * StyleX is not officially compatible with React Native for the Web, but it
 * works anyway.
 *
 * https://github.com/facebook/stylex/issues/280
 */
export type RNfW = never;
