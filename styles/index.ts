// It can't be shared as a component because it's rendered on the document body.
export const bg = "bg-white dark:bg-black";

// It can't be shared as a component because Editor would lose the DOM state.
export const ring = "rounded ring-1 ring-gray-300 dark:ring-gray-800";
